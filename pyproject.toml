[project]
name = "f1tenth-ng"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
docs = [
  "sphinx>=7",
  "myst-parser>=2.0",
  "sphinx-markdown-builder",
]
test = [
  "pytest",
  "pytest-mock",
  "pylint",
]

[tool.setuptools]
packages = []
